# Argocd Namespace Configuration

apiVersion: v1
kind: Namespace
metadata:
  name: argocd
  annotations:
    argocd.argoproj.io/sync-options: Prune=false  # Prevent deletion of resources unless explicitly specified
  creationTimestamp: null
spec: {}

# Security Recommendations
# Ensure to regularly review your RBAC settings within the 'argocd' namespace to prevent over-permissioning. Use the principle of least privilege for access configurations.